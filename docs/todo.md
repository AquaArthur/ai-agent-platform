# 待完成任务清单

本文档基于 `check.md` 提取所有未完成（❌）和需要改进（⚠️）的任务，供项目补充参考。

---

## W02 - 智能体管理

### 任务2：智能体CRUD API实现

#### ❌ 未完成
- **[后端] [前端] 实现公开智能体查询接口**
  - 当前只支持按用户ID查询，缺少公开智能体查询接口
  - 需要添加：`GET /api/agents/public` 接口
  - 查询条件：`status='public'` 的智能体
  - 前端：显示公开智能体列表页面

#### ⚠️ 需要改进
- **[后端] [前端] 实现智能体删除API（软删除）**
  - 当前为物理删除，需要改为软删除
  - 在Agent表中添加 `deleted_at` 字段
  - 修改删除逻辑为更新 `deleted_at` 字段
  - 查询时过滤已删除记录
  - 前端：显示回收站或已删除状态

### 任务3：提示词编排功能实现

#### ❌ 未完成
- **[后端] [前端] 支持变量替换（系统提示词）**
  - 提示词中未实现变量替换功能
  - 需要支持变量如：`{user_name}`, `{date}`, `{context}` 等
  - 实现变量解析和替换逻辑
  - 前端：提供变量输入和预览界面

- **[后端] [前端] 支持变量替换（用户提示词模板）**
  - 提示词模板中未实现变量替换功能
  - 同上，需要实现变量解析和替换
  - 前端：提供变量输入和预览界面

#### ⚠️ 需要改进
- **[后端] [前端] 处理变量替换和上下文注入**
  - 上下文注入已实现，变量替换未实现
  - 需要完善变量替换逻辑
  - 前端：提供变量输入和预览界面

### 任务4：大模型集成实现

#### ❌ 未完成（可选）
- **[后端] [前端] 实现通义千问适配器**
  - 当前只实现了OpenAI适配器
  - 需要实现通义千问API调用适配器
  - 参考：`LlmService` 中的实现方式
  - 前端：在模型选择中显示通义千问选项

### 任务5：插件系统基础实现

#### ⚠️ 需要改进
- **[后端] [插件] 创建AgentPlugin关联表**
  - 当前使用 `Agent.toolsConfig` 字段（JSON数组）存储插件配置对象
  - 建议创建独立的 `agent_plugin` 关联表（可选优化）
  - 当前实现已支持优先级、启用/禁用等配置

---

## W03 - 知识库管理

### 任务3：文本分块服务

#### ⚠️ 需要改进
- **[RAG] 实现智能截断（句号、换行符处截断）**
  - 基础分块已实现，智能截断功能较简单
  - 需要优化：在句号、换行符等自然断点处截断
  - 避免在单词中间截断

### 任务4：文本向量化服务

#### ⚠️ 需要改进
- **[RAG] 实现批量文本向量化（embedBatch方法）**
  - 当前为循环调用单个方法，未实现真正的批量API
  - 需要实现批量调用API，提高效率
  - 位置：`backend/kb/embed_client.py`

#### ❌ 未完成
- **[RAG] 实现API限流控制（添加延迟）**
  - 未找到限流控制实现
  - 需要添加：请求频率限制、延迟重试机制
  - 避免API调用过快导致限流

#### ⚠️ 需要改进
- **[RAG] 实现错误处理和重试机制**
  - 有基础错误处理，但重试机制在Celery任务中
  - 需要在 `embed_client.py` 中实现更完善的重试逻辑

### 任务5：智能体知识库关联

#### ⚠️ 需要改进
- **[后端] [前端] [RAG] 实现检索参数配置（Top-K、相似度阈值等）**
  - 检索参数在知识库配置中，未在关联表中单独配置
  - 建议在 `AgentKnowledgeBase` 表中添加检索参数字段
  - 支持每个智能体-知识库关联的独立检索配置
  - 前端：提供检索参数配置界面

### 任务7：文档处理流程

#### ⚠️ 需要改进
- **[后端] [前端] [RAG] 实现处理进度跟踪（可选）**
  - 有状态跟踪，但无详细进度百分比
  - 需要添加：处理进度百分比（如：30%、60%、100%）
  - 前端显示进度条

---

## W04 - 工作流系统

### 任务1：工作流数据模型设计

#### ⚠️ 需要改进
- **[后端] 实现工作流数据验证（使用JSON Schema）**
  - 当前使用Java Bean Validation，未使用JSON Schema
  - 建议：使用JSON Schema验证工作流节点和边的结构
  - 或保持当前实现，但需要明确说明

---

## W05 - 系统优化与部署

### 任务3：系统测试（必做）

#### ❌ 未完成 - 单元测试编写
- **[后端] 为Service层编写单元测试**
  - 当前只有基础的 `CoreApplicationTests.java`
  - 需要为所有Service层编写单元测试
  - 位置：`backend/core/src/test/java/org/demo/core/service/`

- **[后端] 为Controller层编写单元测试**
  - 缺少Controller层测试
  - 需要为所有Controller编写单元测试
  - 位置：`backend/core/src/test/java/org/demo/core/controller/`

- **[后端] 使用Mockito Mock依赖**
  - 未找到Mockito测试用例
  - 需要在 `pom.xml` 中添加Mockito依赖
  - 使用 `@Mock` 和 `@InjectMocks` 注解

- **[后端] 测试正常流程和异常流程**
  - 缺少测试用例
  - 需要覆盖：正常流程、异常流程、边界条件

#### ❌ 未完成 - 集成测试编写
- **[后端] 编写数据库集成测试**
  - 未找到集成测试代码
  - 需要编写数据库集成测试
  - 使用 `@SpringBootTest` 和 `@Transactional`

- **[后端] 编写API集成测试**
  - 未找到API集成测试代码
  - 需要编写API集成测试
  - 使用 `MockMvc` 或 `TestRestTemplate`

- **[后端] 使用测试数据库或内存数据库**
  - 未找到测试数据库配置
  - 需要配置：H2内存数据库或测试MySQL数据库

- **[后端] 测试后自动清理数据**
  - 未找到测试代码
  - 使用 `@Transactional` 或 `@DirtiesContext` 自动清理

#### ❌ 未完成 - 测试覆盖率
- **[后端] 配置测试覆盖率工具**
  - `pom.xml` 中未配置JaCoCo等覆盖率工具
  - 需要添加JaCoCo Maven插件配置

- **[后端] 生成测试覆盖率报告**
  - 未配置覆盖率工具
  - 运行 `mvn clean test jacoco:report` 生成报告

- **[后端] 确保核心业务逻辑覆盖率>80%**
  - 缺少测试用例
  - 需要补充测试用例，达到80%覆盖率

- **[后端] 补充缺失的测试用例**
  - 缺少测试用例
  - 需要全面补充测试用例

### 任务4：性能优化（必做）

#### ❌ 未完成 - 缓存优化
- **[后端] 实现Redis缓存策略（Cache-Aside）**
  - 未找到Redis缓存实现代码
  - 需要实现Cache-Aside模式
  - 位置：在Service层添加缓存逻辑

- **[后端] 为常用查询添加缓存**
  - 未找到缓存注解或实现
  - 需要为以下查询添加缓存：
    - 智能体列表查询
    - 知识库列表查询
    - 插件列表查询
    - 工作流列表查询
  - 使用Spring Cache `@Cacheable` 注解

- **[后端] 实现缓存更新和失效机制**
  - 未找到缓存实现
  - 需要实现：
    - 创建/更新时清除缓存：`@CacheEvict`
    - 删除时清除缓存：`@CacheEvict`
    - 缓存过期时间配置

#### ⚠️ 需要改进 - 数据库优化
- **[后端] 避免N+1查询问题**
  - 使用MyBatis Plus，但需要检查是否存在N+1问题
  - 需要检查：关联查询是否使用了 `@OneToMany` 或 `@ManyToOne`
  - 使用 `@Select` 或 `@Results` 优化查询

### 任务5：API文档生成（必做）

#### ⚠️ 需要改进 - Swagger配置
- **[后端] 配置API分组**
  - 未明确配置分组，但各Controller有 `@Tag` 注解
  - 建议在 `OpenAPIConfig` 中明确配置API分组
  - 分组示例：智能体管理、知识库管理、工作流管理、插件管理等

#### ⚠️ 需要改进 - API注解
- **[后端] 为响应添加@ApiResponse注解**
  - 部分接口有，但未全部检查
  - 需要为所有接口添加 `@ApiResponse` 注解
  - 包括：200成功、400错误、401未授权、500服务器错误等

#### ⚠️ 需要改进 - 文档验证
- **[后端] 补充接口描述和示例**
  - 有基本描述，示例可能不够完整
  - 需要为所有接口添加详细描述和请求/响应示例
  - 使用 `@ApiOperation` 的 `notes` 和 `examples` 属性

### 任务6：代码优化（必做）

#### ⚠️ 需要改进 - 代码重构
- **[后端] [前端] 提取公共方法，消除代码重复**
  - 有部分公共方法，但需要全面检查
  - 需要检查：Service层、Controller层、Util类
  - 提取重复代码到公共方法或工具类

- **[后端] [前端] 优化代码结构**
  - 代码结构基本合理，但可能还有优化空间
  - 需要检查：包结构、类职责、方法拆分

- **[后端] [前端] 统一代码风格**
  - 基本统一，但需要全面检查
  - 需要统一：命名规范、代码格式、注释风格
  - 可以使用Checkstyle或SpotBugs检查

#### ⚠️ 需要改进 - 日志记录
- **[后端] 配置日志文件滚动**
  - 未找到logback或log4j配置文件，可能使用默认配置
  - 需要创建 `logback-spring.xml` 配置文件
  - 配置：日志文件路径、滚动策略、保留天数、文件大小限制

### 任务7：部署文档编写（必做）

#### ⚠️ 需要改进 - 用户手册
- **[文档/部署] 编写系统使用手册**
  - `docs/README.md` 只有简单说明，可能不够完整
  - 需要编写详细的系统使用手册
  - 包括：系统介绍、功能说明、操作步骤、截图等

- **[文档/部署] 编写功能操作指南**
  - `docs` 目录下有一些文档，但可能不够完整
  - 需要为每个功能模块编写操作指南：
    - 智能体管理操作指南
    - 知识库管理操作指南
    - 工作流管理操作指南
    - 插件管理操作指南

- **[文档/部署] 编写常见问题解答**
  - `deploy/README.md` 中有部分，但可能不够完整
  - 需要编写用户常见问题解答（FAQ）
  - 包括：使用问题、配置问题、故障排查等

#### ❌ 未完成 - 用户手册
- **[文档/部署] 添加截图和示例**
  - 未找到截图
  - 需要为文档添加：
    - 系统界面截图
    - 操作步骤截图
    - 配置示例截图

#### ⚠️ 需要改进 - 开发文档
- **[文档/部署] 编写系统架构文档**
  - `docs` 目录下有一些文档，但可能不够完整
  - 需要编写详细的系统架构文档
  - 包括：系统架构图、技术栈、模块划分、数据流等

- **[文档/部署] 编写开发环境搭建文档**
  - `deploy/README.md` 中有部分，但可能不够完整
  - 需要编写详细的开发环境搭建文档
  - 包括：环境要求、依赖安装、配置说明、启动步骤等

---

## 优先级建议

### 高优先级（必做）
1. **系统测试**（W05任务3）- 核心功能测试，确保系统质量
2. **Redis缓存优化**（W05任务4）- 提升系统性能
3. **软删除功能**（W02任务2）- 数据安全
4. **变量替换功能**（W02任务3）- 核心功能完善

### 中优先级（重要）
1. **批量向量化API**（W03任务4）- 性能优化
2. **API限流控制**（W03任务4）- 稳定性
3. **日志文件滚动配置**（W05任务6）- 运维需求

### 低优先级（可选）
1. **通义千问适配器**（W02任务4）- 可选功能
2. **文档处理进度跟踪**（W03任务7）- 用户体验优化
3. **截图和示例**（W05任务7）- 文档完善

---

## 注意事项

1. **测试相关任务**：建议优先完成单元测试和集成测试，确保代码质量
2. **性能优化**：Redis缓存是重要的性能优化点，建议优先实现
3. **文档完善**：文档是项目的重要组成部分，建议逐步完善
4. **代码质量**：代码重构和风格统一需要持续进行，不是一次性任务

---

## 更新记录

- 2026-01-02: 初始版本，记录未完成任务

